import { FC } from "react";

interface GraphicCardProps {
}

const GraphicCard: FC<GraphicCardProps> = ({  }) => {

    return (
        <div className="flex-1 min-w-[24%] max-w-[24%] p-3 rounded-lg flex items-center justify-between shadow bg-base-300 ">

        </div>
    )
}

export default GraphicCard;
