import { FC } from "react"

interface LogConsultProps {
    id: string
}

const LogConsult: FC<LogConsultProps> = ({  }) => {
    return (
        <div>
            Log
        </div>
    )
}

export default LogConsult;
